Place a replaceable avatar model here:

- VRM: `avatar.vrm`
- GLB or GLTF: `avatar.glb`

The game tries VRM first, then GLB. If neither file exists, it uses the built-in
placeholder character.

VRoid Studio `.vroid` files are project files. Export them as `.vrm` before
placing them in this folder.
